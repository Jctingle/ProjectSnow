{
  "name": "projectsnow",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "wasm:build": "cd wasm-sim && wasm-pack build --target web",
    "postinstall": "npm run wasm:build",
    "dev": "npm run wasm:build && vite --host 0.0.0.0",
    "build": "npm run wasm:build && tsc && vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "@types/three": "^0.184.1",
    "typescript": "~6.0.2",
    "vite": "^8.1.0"
  },
  "dependencies": {
    "three": "^0.185.0",
    "wasm-sim": "file:./wasm-sim/pkg"
  }
}
